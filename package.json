{
  "name": "tree-sitter-yscript",
  "version": "1.0.0",
  "description": "tree-sitter parser for yscript",
  "main": "bindings/node",
  "scripts": {
    "build:grammar": "tree-sitter generate",
    "build:types": "node ./node_modules/@asgerf/dts-tree-sitter/build/src/index.js src > index.d.ts",
    "build:wasm": "tree-sitter build-wasm",
    "build": "npm run build:grammar && npm run build:types && npm run build:wasm",
    "test": "tree-sitter test"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "nan": "^2.16.0"
  },
  "devDependencies": {
    "@asgerf/dts-tree-sitter": "^0.1.0",
    "tree-sitter-cli": "^0.20.6"
  }
}
